namespace Scanner
{
    public enum TokenType
    {
        NUM,
        ID,
        OP,
        VAR,
        BEGIN,
        END,
        IF,
        ELSE,
        LP, // (
        RP, // )
        ERROR,
        DOT, 
        ASSIGN,
        COMMA
    }
}